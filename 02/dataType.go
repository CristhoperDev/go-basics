package main

import "fmt"

func main() {
	/*var a int
	var b int8
	name := "Cristhoper"
	lastName := "Alvarez"
	age := 21
	a = 121212
	b = 5

	// casting
	c := a + int(b)
	fmt.Println(c)
	fmt.Printf("Hello %s %s %d how are you?\n", lastName, name, age)
	fmt.Printf("b is type of: %T\n", b)*/
	//arithmetic priority
	// + - * /
	// () % * / + -
	//modulo is the residue of division
	// 7 % 3 = 1
	//d := 6 + 6*(6-6)
	//fmt.Println(d)

	//zero value
	var name string
	var number int
	var flag bool
	fmt.Println(name, number, flag)
}
